[package]
name = "boitata-store"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Embedded SQLite. `bundled` compiles SQLite in, so there's no system-library
# dependency and the schema behaves identically everywhere. Opted in here (not
# at the workspace level) so only this crate pays the from-source build.
rusqlite = { workspace = true, features = ["bundled"] }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
tokio-test = { workspace = true }
tempfile = { workspace = true }
